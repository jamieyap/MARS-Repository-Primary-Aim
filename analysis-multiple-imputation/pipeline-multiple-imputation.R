rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "create-wide-format-dataset-for-mi.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-baseline.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-01.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-02.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-03.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-04.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-05.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-06.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-07.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-08.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-09.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-10.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-11.R"))
rm(list = ls())

source(file = file.path("analysis-multiple-imputation", "impute-block-12.R"))
rm(list = ls())

